# NLP Semantic Similarity
Project ini menggunakan model **DistilBERT** untuk menghitung kemiripan kata dalam kalimat.

### Cara Menjalankan:
1. Clone repo ini.
2. Install library: `pip install -r requirements.txt`.
3. Jalankan: `python app.py`.
